"""Concrete channel adapters (AI-2.5.3+).

Each package here implements :class:`~libs.adapter.ChannelAdapter` for one
channel. The sync engine looks them up by ``code`` (matches
``channels.code`` in the DB, ADR-0018).
"""
