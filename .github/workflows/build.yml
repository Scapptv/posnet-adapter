name: build

on:
  push:
    branches: [main]

jobs:
  compose-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: docker compose config validation
        run: docker compose config -q
  # Qeyd: core servis image build + GHCR push AI-1-de (services/core/Dockerfile yarandiqda)
  # elave olunacaq. Hal-hazirda yalniz compose validasiyasi.
